apiVersion: v1
kind: Service
metadata:
  name: raft-fastapi
spec:
  type: LoadBalancer  # Change to ClusterIP if running inside a local cluster
  selector:
    app: raft
  ports:
    - protocol: TCP
      port: 8000  # Exposes FastAPI service externally
      targetPort: 8000
