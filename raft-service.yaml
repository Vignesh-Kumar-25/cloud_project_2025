apiVersion: v1
kind: Service
metadata:
  name: raft
  labels:
    app: raft
spec:
  clusterIP: None  # Headless service for StatefulSet DNS resolution
  selector:
    app: raft
  ports:
    - name: raft-port
      port: 7010
    - name: raft-internal
      port: 7011
